package co.edu.uptc.view.scoreBoard;

import javax.swing.*;
import java.awt.*;

public class ScorePanel extends JPanel {
    public ScorePanel() {
        setPreferredSize(new Dimension(300, 183));
        setBackground(new Color(180, 100, 100));
    }
}
